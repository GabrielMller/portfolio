name: Deploy Monitoring Stack

on:
  push:
    branches: [ "main" ]
    paths:
      - 'infra/monitoring/**'
  workflow_dispatch:

jobs:
  monitoring-deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Prepare Monitoring Namespace
        run: |
          export KUBECONFIG=/etc/rancher/k3s/k3s.yaml
          kubectl create namespace monitoring --dry-run=client -o yaml | kubectl apply -f -

      - name: Apply Prometheus Stack
        run: |
          export KUBECONFIG=/etc/rancher/k3s/k3s.yaml
          # Aplica ConfigMap, Deployment e Service de uma vez
          kubectl apply -f infra/monitoring/prometheus-stack.yaml
          
          # Força o restart para carregar novas configurações do ConfigMap
          kubectl rollout restart deployment/prometheus -n monitoring

      - name: Verify Deployment
        run: |
          export KUBECONFIG=/etc/rancher/k3s/k3s.yaml
          echo "Verificando pods no namespace monitoring..."
          kubectl get pods -n monitoring
          echo "Verificando serviços..."
          kubectl get svc -n monitoring